/***/

#include <stdio.h>

int main()
{
   printf("O tamanho de bytes de um char\t=\t%ld\n", sizeof(char));
   printf("O tamanho de bytes de um int\t=\t%ld\n", sizeof(int));
   printf("O tamanho de bytes de um float\t=\t%ld\n", sizeof(float));
   printf("O tamanho de bytes de um double\t=\t%ld\n", sizeof(double));
}
